import { fontWeight, variants } from './typography';
import colors from './colors';

export default {
  colors,
  screenSize: {
    mobileS: '480px',
  },
  text: {
    fontWeight,
    variants,
  },
};
